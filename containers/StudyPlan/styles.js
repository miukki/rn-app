import { StyleSheet } from 'react-native'

export default StyleSheet.create({

  planSelector: {
    flex: 1
  },

  Button: {
    margin: 30,
    marginTop: 0,
    marginBottom: 0,
    borderRadius: 8
  }

});
